#include <stdio.h>

int main() {
    char str[100];
    int count = 0;
    
    // Input a string
    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);

    // Count characters until null terminator or newline
    while (str[count]
